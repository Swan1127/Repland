# Repland 产品思路记录

> 这是一份持续更新的工作文档，不是最终产品需求文档。每轮讨论后，已确认的决定会写入“已确认决定”，未决问题会保留在“待确认问题”。

## 项目基本信息

- 产品名称：Repland
- 目标平台：Android App
- 核心理念：帮助用户梳理每天的任务和目标，并生成计划表。
- 项目背景（已确认的产品判断）：制定计划需要同时权衡任务、时间、目标与现实约束，往往消耗大量脑力和时间；而好的计划又是用户开始行动、取得成果的重要前提。现有待办和计划类 App 常停留在任务堆叠，难以随现实变化动态、个性化地调整。即使借助通用 Agent 制定计划，用户通常仍需多轮对话，得到的也往往是一份固定计划书。
- Repland 的问题定义：降低制定计划的时间、脑力与选择成本，以内置的 Agent 工作流持续生成并调整个性化计划，避免让用户反复通过对话维护一份静态计划书。
- 共创方式：通过持续提问逐步明确产品定位、用户流程、功能范围、交互方式和实现思路。

## 已确认决定

1. 长期面向：所有容易拖延、感到任务失控或难以做出合理规划的人群。
2. 第一批核心用户：大一新生。产品帮助他们安排学习与生活计划、形成执行习惯，并降低规划本身的脑力成本。
3. 主要打开时机：接到新任务后；计划失控、需要调整时；其余时间按需打开。
4. 核心价值：将一堆任务安排进计划表，并以算法和 AI 结合任务事项、目标类型和优先级，判断何事更重要并生成计划，帮助用户更高效地制定和执行计划。
5. 产品主方向：今日计划生成器是核心；重新规划和将模糊目标拆为可执行任务是辅助功能，而不是产品主定位。
6. 习惯培养：面向有需要的部分人群（尤其大一新生）的可选能力。后续通过用户的执行反馈识别真实节奏与可改善之处，并结合 AI 给出建议，帮助形成习惯；它不应成为所有用户的必经流程。
7. AI 的角色：AI 参与理解任务、识别目标类型、辅助判断优先级和生成计划；可解释的规则仍需保证计划的可行性与用户控制权。
8. 第一版核心范围：任务汇总与计划任务表。用户将任务集中交给 Repland，系统对其排序并生成计划。
9. 当前产品定位为任务规划工具；长期规划将在产品能力成熟后作为独立模块加入，而非当前任务规划功能的一部分。
10. 当任务总量超过可用时间、且无法在截止前完成时，系统必须明确告知计划不可完全实现，并帮助用户在轮换、延期、拆分、降低目标或放弃之间取舍。
11. 任务完成、延期和取消状态只能由用户自行改变；每天计划结束后，AI 可结合用户画像和当天任务状态生成快速总结，但不得改变任务状态。
12. 总原则为“用户主导、可授权的 AI 决策”：默认由用户保留任务、目标和计划的最终控制权；当用户没有明确偏好时，可主动授权 AI 在既定范围内决定规划取舍。AI 始终不得违反硬约束、用户锁定事项和任务状态由用户维护的边界，且计划应可回退。
13. 项目成功状态：用户对自己需要完成的事情有清晰认知，在复杂计划面前不再手忙脚乱或感到迷茫，并逐步形成更稳定的主动行动意愿与自驱力。
14. Repland 是规划与执行助手：通过通知提醒和适度监督帮助用户回到计划，但不能替用户完成现实任务，也不能代替用户判断任务是否完成。

## 当前待确认问题

1. 对大一新生，第一版优先涵盖学习、课程作业、考试复习、社团/生活事务和习惯中的哪些内容？
2. 用户如何提供不可移动时间：手动填写课程表、导入课表/日历，还是两者都支持？
3. 计划表需要具体到时间块、顺序任务，还是两者结合？
4. 用户如何表达目标类型和优先级，AI 在哪些情况下可以推断、哪些情况下必须询问？
5. AI 生成计划后，应让用户先确认、直接生成后修改，还是提供多个方案？
6. 任务超过可用时间时，系统如何呈现取舍和建议？
7. 习惯培养模块以何种形式出现：主动设置习惯、AI 发现问题后推荐，还是由用户按需开启？
8. 首版是否需要通知、课表/日历同步、账号与跨设备同步，以及云端 AI？

## 工作假设

- 重新规划是已确认的辅助功能；其具体触发方式和交互仍未确认。
- 对大一新生的具体场景尚待细化，不能仅以“所有拖延者”作为首版设计依据。

## 候选产品方向（未确认）

### A. 今日计划生成器（已确认为核心方向）

- 用户输入今天要做的事、截止时间和可用时间。
- Repland 自动排序、估算时长并生成时间块。
- 重点价值：把任务清单变成可执行的今日安排。
- 主要风险：容易变成普通待办或日历工具。

### B. 目标拆解教练（已确认为辅助能力）

- 用户输入一个长期目标，Repland 逐步拆成阶段目标、近期任务和今天的行动。
- 重点价值：解决“知道想做什么，但不知道今天做什么”。
- 主要风险：目标拆解质量和持续使用动力要求较高。

### C. 现实重排助手（已确认为辅助能力）

- 用户先生成计划，执行中遇到加班、拖延或临时事务时，用很少的操作告诉 Repland。
- Repland 重新安排剩余任务，并解释哪些任务被保留、推迟或舍弃。
- 重点价值：让计划能适应现实，而不是计划失败后让用户自责。
- 主要风险：需要可靠地处理时间冲突、优先级和未完成任务。

### 可能的组合方向（未确认）

- 当前共识已取代原先的假设：A 是产品核心；B 和 C 用来提升计划质量、执行与调整体验。
- 后续需要确认 B、C 在 MVP 中的具体深度，而不是再讨论三者谁是主方向。

## 暂定设计草案（待用户确认，可推翻）

### 一句话定位

帮助大一新生把学习和生活中的一堆任务，快速转成更合理、更容易执行的日计划。

### 暂定核心循环

接收新任务或目标 → AI 与规则识别类型和优先级 → 生成今日计划 → 执行与标记 → 必要时重新规划 → 按需复盘或进入习惯支持。

### 暂定首次使用入口

- 用户用自然语言输入：“今天要完成报告、去健身、买日用品，报告最重要。”
- Repland 只追问生成计划所必需的信息，例如截止时间、预计时长和不可用时间。
- App 生成可调整的时间块，而不是要求用户先建立复杂的项目体系。

### 暂定差异化假设

- Repland 的首要价值是替用户完成“从任务堆到可执行计划”的高脑力规划工作。
- 计划被打乱后的重新规划是重要保障能力，不取代自动计划生成这一核心。
- 面向大一新生时，学习任务、课程节奏和习惯形成可能构成更具体的差异化，尚待验证。

### 暂定 MVP 边界

- 任务快速录入
- 截止时间、预计时长、优先级和可用时间
- 今日计划自动生成
- 时间块拖动或手动调整
- 未完成任务的一键重新规划
- 计划执行状态和基础提醒

暂不纳入 MVP 的方向：复杂项目管理、社交协作、过度复杂的长期目标树、排行榜和装饰性功能。

### 暂定一日使用流程

1. 用户快速记录今天想做的事情，可以使用自然语言。
2. Repland 提取任务、截止时间、预计时长和优先级；只针对缺失且必要的信息提问。
3. 用户确认今天的可用时间，或直接采用默认可用时间。
4. Repland 展示“必须完成”和“如果有时间再做”两层计划，并可选生成时间块。
5. 用户通过完成、跳过、延后或“被打断”更新状态。
6. 发生变化时，用户点击“重新规划”，Repland 说明哪些任务被保留、移动或舍弃。
7. 晚间只做轻量复盘：完成了什么、未完成的原因、明天是否需要继续。

### 暂定产品原则

- 输入成本低于维护成本：用户不应为了获得计划而填写大量字段。
- 计划是建议而非承诺：用户始终可以修改、跳过或拒绝安排。
- 先保护最重要的事情：时间不足时，主动暴露取舍，不假装所有任务都能完成。
- 计划失败不等于用户失败：重新规划应当是正常操作，而不是惩罚或负面评价。
- AI 的关键判断要可解释：至少说明安排依据以及被延后的任务。

### 核心用户叙事（已确认方向，场景细节待验证）

> 我刚进入大学，课程、作业、复习、生活琐事和想培养的习惯一下子变多。我不知道该把哪些事放进今天，也不想花很多时间自己排计划。Repland 帮我判断轻重缓急，生成可执行的安排；计划变化时，再帮我调整。

### 暂定成功标准（未确认）

- 大一新生能在接到任务后，快速把任务加入并获得可执行的计划。
- 用户能理解 AI 为什么判断某些学习或生活任务更重要。
- 用户制定计划所需的时间和脑力显著降低。
- 用户能按计划推进重要事项；计划被打乱后能继续调整而非放弃。
- 对主动开启习惯支持的用户，产品帮助形成更稳定的学习与生活执行节奏。

## 暂定首页草案（未确认）

### 页面结构

1. 顶部：日期、当天剩余可用时间，以及一句轻量状态提示。
2. 首要入口：大而醒目的输入框“告诉我今天要做什么”，旁边提供语音入口。
3. 计划主体：按“现在/下一步”“今日必须完成”“有时间再做”组织任务；在用户提供时间条件后显示时间块。
4. 当前行动卡：只突出一个最推荐的下一步，避免用户面对过长清单。
5. 重排入口：当用户说“我被打断了”“今天来不及了”或点击按钮时，进入快速重新规划。
6. 底部导航候选：今日、任务、目标、复盘；也可能压缩为今日和历史两个入口，待验证。

### 暂定交互原则

- 首页首先帮助用户行动，而不是展示统计数据。
- 任务列表默认控制长度，未安排的任务折叠显示。
- 任何自动安排都提供“为什么这样排”的解释入口。
- “重新规划”必须比编辑多个任务的时间和步骤更少。

## 计划表展示与渐进日程生成（已确认方向）

- 用户打开当天计划时，默认首先看到按动态优先级排列的任务清单，而不是具体时间日程。
- AI 参考后续任务完成情况、实际耗时和用户/学习画像，逐步优化并生成更具体的时间段日程。
- 新用户或刚生成的初步计划，先以任务排序为主，并将任务暂时插入课程等固定事件之外的空闲时段。
- 初步插入的时间安排因缺少个人执行反馈而参考性较弱，应被视为可调整的初步建议，而非强制日程。
- 随着反馈累积，具体时间段日程的个性化程度和可信度提高；可信度的展示方式尚待确认。
- 第一版由用户设置休息时间；课程表和休息时间共同界定不可安排范围，AI 只在其余可用时间内安排任务。

### 已确认的任务清单行信息

- 任务名称。
- 剩余天数：距任务截止日期的天数。
- 动态优先级仍作为已确认的排序标注保留，但应保持轻量，不挤占任务名称和剩余天数的阅读空间。
- 没有截止日期时，明确应在当天完成的短期任务显示“本日内”。
- 未给出时间信息的任务显示“待定”。
- “待定”任务不参与 AI 自动排程；用户补充必要时间信息后才参与排序与计划生成。

## 当前方案的重大待解决问题（产品风险审视）

以下不是已确认的产品决定，而是实施前或早期验证中必须处理的风险。

### 1. 首次输入与日常反馈的负担

当前方案要求用户录入课表、休息模板，并为任务提供不同层级的信息；每次完成任务还要写简短总结。若输入步骤过多，会与“降低规划时间和脑力成本”的核心价值冲突。需要设计明确的最小必填信息、默认值与渐进补充机制。

### 2. AI 权重的可解释性与稳定性

已确认的 45% AI 规划适配度和 15% AI 情境重要性，仍需要定义可复核的结构化输入、输出与失败回退。大模型应提供判断依据，确定性规则负责硬约束和可行性，避免模型给出看似精确但不可解释或前后不一致的排序。

### 3. 动态优先级的变化边界

触发因素已经确定，但提升阈值、更新频率、一次允许变化的幅度，以及何时提示用户仍未确定。若变化太频繁，计划表会失去稳定性；太迟变化，又会错过真正紧急的任务。

### 4. 时间估算与不可行计划

计划质量依赖预计时长、跨天拆分和可用时间。需要决定 AI 与用户估时不一致时如何展示、要留多少缓冲、以及在一个月内仍无法满足截止时间时如何明确告警和帮助取舍。

### 5. 学习画像的隐私与用户控制

任务总结、成绩和执行历史属于高度个人化数据。必须明确数据保存位置、是否发送给云端 AI、授权提示、查看/修改/删除画像的方法，以及删除后 AI 是否立即停止参考。

### 6. 计划版本与执行状态的规模

每次确认改动都创建版本且可回退，任务状态也会随版本恢复。需定义历史版本保留期限、版本列表的呈现方式，以及回退后与近 7 天执行日志的关系，避免历史过多或用户误以为已完成任务永久丢失。

### 7. MVP 范围需要再收束

当前已包含任务排序、课表、休息模板、跨天排程、AI 分析、学习画像、完成总结、版本回退与倒数提醒。它们共同构成完整愿景，但第一版仍需划分“必须可用”与“验证后加入”，否则开发和验证周期会过长。

## 第一版新建任务的最小输入（已确认方向）

- 必填：任务简介、初始优先级、时间长度。
- 任务名称由 AI 从任务简介中提取/生成。
- 任务简介也是 AI 理解任务内容、难度与推进方式的主要上下文。
- 用户能否编辑 AI 生成的任务名称，尚待确认。
- “时间长度”指用户预计完成该任务所需的天数；此前跨天任务机制与此一致。

## 文档粒度约定（已确认）

- 当前阶段以粗略产品思路为主，不继续展开软件技术、数据字段、算法阈值或具体交互实现。
- 已记录的细化原则保留在本文件和阶段性接力文档中，待后续需要时再展开。
- [REPLAND_ROUGH_IDEA.md](REPLAND_ROUGH_IDEA.md) 作为当前产品思路的简明版本。

## 功能阶段取舍（已确认）

- 习惯培养不进入第一版的必须功能。
- 该思路保留：在任务规划核心体验得到验证后，可面向有需要的用户引入行为与心理学参考、完成反馈和 AI 支持的习惯培养能力。

## AI 与 Agent 的核心思路（已确认）

- 第一版为单设备个人规划工具，不要求账号或跨设备同步。
- 软件以 Agent 作为稳定工作流基座，将任务理解、排序、硬约束检查、计划建议、变更预览和版本确认等流程固化其中。
- AI 模型以 API 形式参与具体分析与建议，而非直接决定整个产品流程。
- Agent 负责保持工作流和输出标准相对稳定，尽量降低后续接入不同 AI 模型时的体验差异。
- 第一版的模型选择对用户透明，由 Repland 在后台统一管理；用户无需配置 API 或自行选择模型。

### 已确认的容量不足与跨天插入规则

- 当当天可用时间不足以容纳全部任务时，系统自动按天循环插入计划表，而不是只保留当日冲突。
- 短期任务优先插入近期可用的计划时段。
- 长期任务在计划冲突或容量不足时，可以在多天之间轮换分配工作量。
- 自动跨天插入和轮换必须遵守硬约束、截止日期、用户设定的每日目标和可用时间。
- 用户始终可以手动调整任务所在日期、时间和分配方式。
- 预计超过一个月完成的任务可以正常加入并按天拆分、跨天安排。
- 未来一个月是计划表的可见滚动窗口：系统只向用户展示该任务在最近一个月内的每日事项，超过窗口的每日安排不显示。
- 因此，一个月限制的是日程展示范围，而非长期任务的拆分与规划能力。
- 短期/长期任务同时依据用户填写的预计完成天数，以及 AI 对任务耗时和拆分需求的判断。
- 当 AI 推荐的任务周期与用户填写的预计完成天数不一致时，实际排程优先使用用户填写的周期。
- AI 可以展示建议周期及理由，但不自动使用；用户在后续执行中可根据自身感受手动调整。

## 竞品验证与产品启示（2026-09-11）

### 已观察到的市场能力

- Todoist 已在 Android 支持自然语言添加任务、日历时间块、任务拖拽排程与日历同步。
- Motion 已提供按可用时间、任务时长、截止时间、优先级和日历变化自动排程，并在变化后重新优化。
- Structured 主打将任务和待办合并成单一的可视化时间线。
- Sunsama 已提供引导式日计划、工作量预览、时间估算、延后非必要任务、时间块及自动重排。

参考：

- https://www.todoist.com/help/todoist/get-started/time-blocking-in-todoist-d6Pf1uTpc
- https://www.usemotion.com/help/time-management/auto-scheduling
- https://structured.app/
- https://www.sunsama.com/features/timeboxing
- https://help.sunsama.com/docs/getting-started/basics/daily-planning-the-basics/

### 暂定产品启示（推断，未确认）

- “支持任务、日历、时间块或自动排程”本身不足以构成 Repland 的差异化。
- Repland 更值得验证的切入点是：用户被打断或拖延后，用极低摩擦恢复计划；App 清楚呈现取舍，并始终给出一个可以立即开始的下一步。
- 这一切入点仍需要由首批用户和真实场景验证，不能直接视为最终定位。

## “恢复模式”候选体验（未确认）

### 触发

- 用户手动点击“我被打断了”或“今天来不及了”。
- 已排定任务超时仍未完成。
- 新增的日历事件与计划冲突。

### 交互流程

1. Repland 先确认事实：还剩多少可用时间、哪些任务必须保留、这次变化预计影响多久。
2. App 展示取舍，而非静默移动任务：例如“保留报告和缴费；跑步改到明天；阅读移入待处理”。
3. 用户只需确认一个推荐方案，或手动锁定/延后一个任务。
4. 首页随即回到一个明确的“现在开始”任务，而不是重新展示长清单。

### 关键约束

- 不擅自改变用户锁定的任务或硬截止任务。
- 不把所有事情塞进剩余时间；时间不足时必须明确提出舍弃或延后。
- 每次重排需要有一句理由，让用户理解代价。
- 用户应能在极少操作内退出恢复模式并继续行动。

### 待验证的魔法时刻

下午计划被会议打断后，用户打开 Repland，看见“今天还剩 2 小时 20 分钟。保留报告和缴费，跑步改到明天；现在先用 45 分钟写报告第一部分。”用户确认后立即进入任务，而不是重新手动改整天日程。

## 任务排序模型（正在共创）

### 已确认的排序因素

- 用户对单个任务设置的初始主观优先级。
- 任务类别，例如学习、工作、家务、游戏等。
- 用户可修改各任务类别的默认优先级；类别不应由系统永久固定排序。
- 所有任务由排序算法与 AI 大模型协同分析、排序；AI 需要结合任务内容、目标类型、类别和用户信息给出判断，算法负责把判断转为可执行计划。
- AI 任务规划适配度：大模型分析任务难易程度、推进方式和目标发展节奏，判断任务更适合短期还是长期推进、需要集中整块时间还是拆为多段小时间完成。

### 已确认的硬约束

- 临近截止时间的任务：默认定义为距离任务截止时间不足 3 天。
- 用户标记为“不可避免”的任务；例如固定课程时间或其他不能跳过的事情。用户可通过优先级选择将这类任务从软约束提升为硬约束。
- 第一版首次使用时，学生录入每周课表；课程自动成为重复、固定时间的“不可避免”任务。

### 已确认的用户主观优先级机制

- 用户为单个任务设置四档初始优先级：不可避免、高、中、低；其中“不可避免”是最高优先级选择，同时使任务成为硬约束。
- 默认值为“中”。
- 高、中、低三档初始优先级占软约束评分的 25%，表达用户最初的主观判断；“不可避免”不参与该项加权。
- 计划执行过程中，排序优先级会随时间流逝动态变化；计划表展示动态优先级，而非只展示用户最初设置的高、中、低。
- 系统应保留初始优先级，不能在后台静默覆盖用户意图；动态变化应显示原因，例如截止时间临近、等待时间增加或可用时间减少。

### 已确认的动态优先级触发因素

- 临近任务截止时间。
- 临近系统为任务安排的执行时段。
- 任务被多次延后。
- 被多次延后的任务除提高动态优先级外，系统还应提醒用户重新调整该任务目标，避免只靠不断加急掩盖目标不合理的问题。

### 新任务加入时的全局排序（已确认方向，约束待细化）

- 用户不断加入新任务时，AI 大模型需要能够同时比较新任务的初始优先级与旧任务的动态优先级，重新排序全部未完成任务。
- 重排不能只看新任务本身：模型需读取现有任务的截止风险、当前动态优先级、延后历史、计划时段、目标关联和用户初始意图。
- 旧任务动态优先级的变化必须受到稳定性约束，避免计划表因每个新任务而频繁跳动；具体约束规则尚待确认。
- 每次由新任务导致的重要排序变化，系统应说明被影响的旧任务和主要原因。

### 已确认的新任务冲突处理流程

1. 新任务先加入计划表，并保留其与既有计划的冲突，不静默移动旧任务。
2. 系统在用户空闲时提示其确认冲突；“用户空闲”的具体判定方式尚待确认。
3. 用户可选择自己手动排序，或选择让 AI 对新旧任务重新排序。
4. AI 只有在用户选择 AI 排序后才生成重排建议；系统先展示受影响任务、变动内容和主要原因，用户确认后才应用到计划表。

### 已确认的计划预览与版本历史

- AI 排序结果必须先预览、再由用户确认应用，不能直接覆盖当前计划。
- 原计划必须保留为可恢复版本。
- 用户或 AI 的每一次已确认计划改动，都创建一个可查看、可恢复的计划历史版本。
- 用户可以选择回退到任一历史版本。
- 建议采用非破坏性回退：恢复旧版本时创建新的当前版本，同时保留之后的历史版本，避免回退行为丢失信息；该实现细节待最终确认。

### 已确认的执行状态日志与版本状态

- 任务的完成、取消、延期等执行状态变化记录在每日日志中。
- 每日日志只保留最近一周的状态变化，用于近期复盘。
- 已留存的历史计划版本也必须记录版本创建当时的任务执行状态。
- 用户查看或回退历史计划时，应能保留并看到该版本当时的任务状态；历史版本的具体留存期限尚待确认。
- 用户回退到旧计划版本时，任务的执行状态也恢复为该版本当时的状态。
- 回退应生成新的当前版本，从而保留用户回退前的计划与执行状态。

### 动态优先级稳定性约束（候选规则，未确认）

- 硬约束、用户锁定任务和正在执行/即将执行的任务不应被普通新任务挤掉。
- 无硬约束触发时，旧任务的动态优先级不应在一次重算中发生过大的跳变。
- 已因截止风险或多次延后而升级的旧任务，不应被普通新任务无解释地下调。
- AI 应提出旧任务延后或降级的建议与后果；用户已确认新任务冲突不能在未授权时触发自动调整。

### 已确认的临近截止交互

- 用户可以手动调整任务进入“临近截止”状态的日期/期限。
- 系统提供倒数日功能，用剩余天数提醒用户该任务的截止风险。
- 临近截止状态会触发硬约束处理；具体是否在倒数期间持续提升排序权重，仍待确认。

### 设计原则

- 硬约束不作为“很高的分数”参与普通竞争：系统必须先保证临近截止和“不可避免”任务获得可行安排。
- 对有固定起止时间的“不可避免”任务，系统直接预留对应时间块；对没有固定时间的“不可避免”任务，系统优先为其寻找可行时段。
- 对可灵活安排的任务，再根据各因素的权重得出排序分数。
- 系统应向用户解释某任务被排在前面的主要原因，并允许用户覆盖建议。
- 除已确认的 AI 任务规划适配度外，其余软约束权重需要通过后续提问确定。

### 已确认的软约束权重

- 在排除硬约束后，AI 任务规划适配度占软约束评分的 45%。
- 用户对单个任务设置的高、中、低主观优先级占 25%；“不可避免”属于硬约束，不参与该项加权。
- 用户可配置的任务类别优先级暂按回答顺序记录为 15%。
- AI 补充情境重要性占 15%，由大模型综合判断任务与长期目标的关联、被拖延次数、课程/成绩影响及其他重要因素。
- 上述 45% / 25% / 15% / 15%构成软约束评分的 100%。

### 已确认的默认任务类别画像

第一版面向大一新生，预置以下任务类别。它们的比例是“任务类别优先级”这 15%因素内部的默认相对比例，并非总排序分数：

| 类别 | 类别因素内的默认比例 |
|---|---:|
| 课内学习 | 30% |
| 课外学习 | 25% |
| 办公事务 | 25% |
| 休闲娱乐 | 20% |

- 用户仍可修改各类别的优先级，类别默认值只提供面向大一新生的初始倾向。
- “办公事务”的具体边界（例如校园行政、学生组织、兼职或个人文书）尚待确认。

### AI 任务规划适配度的输出（待细化）

- 难易程度和预估认知负荷。
- 建议推进周期：短期集中完成，或在更长周期持续推进。
- 建议执行形态：连续专注时间块，或可拆分的小时间块。
- 需要时的任务拆分建议及其理由。
- 对任务周期的判断：结合用户填写的预计完成天数、任务耗时与拆分需求，给出短期/长期建议。

AI 应输出可解释的结构化建议，而不是只给出一个不可见的分数；最终时间安排仍需通过算法检查可用时间、硬约束和用户选择。

### AI 补充情境重要性（已确认，具体规则待细化）

- 大模型综合任务与长期目标的关联、被拖延次数、课程/成绩影响及其他重要信号，给出该任务的补充重要性判断。
- 被拖延次数、课程信息、成绩影响和用户历史等客观信号，应由系统保存并提供给大模型；大模型负责综合解释，不应凭空编造这些事实。
- 该因素与“AI 任务规划适配度”分工不同：前者判断任务该如何推进，后者判断任务在用户当前处境中的附加重要性。

### 学生学习画像（已确认方向，字段与交互待细化）

- 用户完成所有类别的任务后，都需要留下简短总结。
- 学生可输入学习结果和成绩。
- 所有任务完成总结与实际执行记录共同构建通用用户画像；学习任务总结、学习结果和成绩额外构建学生学习画像。
- 通用用户画像和学习画像都是 AI 模型的重要参考输入，用于辅助判断任务的规划适配度和补充情境重要性。
- 画像应以用户实际输入和系统实际记录为依据，避免 AI 凭空推断用户能力、习惯或学习状态；具体隐私、修改和删除机制尚待确认。

### 已确认的任务完成总结内容

- 最低必填项：用户说明“完成了什么”。
- 可补充项：实际用了多长时间、具体完成了什么、该任务已完成的进度百分比。
- 更完整的总结应同时记录结果、实际耗时和任务进度，以更新用户画像和后续计划判断。

### 已确认的跨天任务机制

- 用户创建任务时需要注明预计需要完成的天数，作为 Repland 与 AI 规划的参考。
- 默认预计天数为 1 天，即任务默认应在当天完成。
- 对预计需要多天完成的长期任务，系统按任务进度将剩余部分继续顺延到下一天。
- 若用户提交总结时任务进度不足 100%，任务携带已完成进度和剩余工作延续到下一天，而不是被视为丢失或重新创建。
- 预计天数、实际进度、实际耗时和剩余工作共同作为后续动态优先级与时间安排的参考。
- 跨天任务同时支持两种每日分配方式：用户自行设定每日目标，或由 AI 根据总预计天数自动切分每日工作量。
- AI 自动切分后，会根据用户的完成总结、实际进度、实际耗时和由此构建的用户/学习画像，持续调整后续每日工作量。
- 当用户设定的每日目标与 AI 建议冲突时，AI 只能提出调整建议，必须等待用户确认，不能自动改变用户每日目标。
- 经用户确认的每日目标调整应生成新的计划历史版本。

### 已确认的任务详细介绍

- 任务加入计划表时，用户可以补充任务的详细介绍。
- 详细介绍作为 AI 任务规划适配度分析的重要参考资料，用于理解任务难度、推进方式、短期/长期属性和集中/分段执行建议。
- AI 任务规划适配度维持已确认的 45%；用户提到“40%”属于口误。

### 待补齐的候选因素（均未确认）

- 截止时间距离和逾期风险（默认 3 天内为临近截止；具体加权方式待定义）。
- 任务的预计时长与当天剩余可用时间。
- 是否关联学期目标、课程成绩或长期目标。
- 是否依赖另一个任务先完成。
- 是否为用户主动锁定的任务。
- 任务是否已经被多次推迟。
- 用户的可用时段、课程表和固定事件。
- 用户的精力/专注时段与任务难度匹配。

## MVP 实现思路（待确认）

### 技术取向

- 原生 Android：Kotlin + Jetpack Compose，优先保证任务录入、计划查看、课表/日程冲突处理和通知体验。
- 本地优先仍是技术候选；账号、同步和云端 AI 的边界尚未确认。
- 算法与 AI 协同：规则引擎保证固定时间、截止时间和可用时间等硬约束；AI 用于理解任务、识别目标类型、辅助判断优先级、提出计划和解释建议。
- 不将所有排程判断完全交给大模型：用户仍可修改计划，系统须展示关键安排依据。

### 最小领域模型

- Task：任务名称（AI 从简介生成）、任务简介、时间长度、预计完成天数（与时间长度的关系待澄清）、当前进度、用户每日目标（可选）、初始主观优先级（高/中/低/不可避免）、任务类别、截止时间、固定起止时间、状态、是否锁定、是否可延后。
- WeeklyCourseSchedule：课程名称、周几、起止时间、地点/备注（如有）、重复规则；生成固定的不可避免时间块。
- PlanBlock / 排序结果：动态优先级、动态优先级变化原因、推荐执行时段与排序解释。
- PriorityChange：触发事件（时间流逝/新任务/延后）、变化前后优先级、受影响任务、AI 理由与用户是否确认。
- PlanVersion：版本号、父版本、完整计划快照或变更集、创建时间、变更来源（用户/AI）、确认状态、变更理由与可恢复状态。
- DailyExecutionLog：任务、状态变化前后值、发生时间、变化原因；仅保留最近 7 天。
- CompletionSummary：最低必填的完成内容、实际耗时（可选）、详细完成结果（可选）、任务进度百分比（可选）、学习结果（如适用）、用户输入时间。
- UserProfile：由所有任务的完成总结和实际执行记录构成，供 AI 参考。
- StudentLearningProfile：由课表、学习任务总结、学习结果、成绩和可解释的系统记录构成，供 AI 参考。
- AvailabilityWindow：由课表、用户设置的休息时间、固定事件和休息缓冲共同计算出的可安排时间范围。
- RestPeriod：用户设置的不可安排休息时段；以每周重复的固定模板为主，同时支持对当天单独临时修改。
- PlanBlock：某任务在某段时间内的建议安排，区分软安排与用户锁定安排。
- ReplanEvent：打断类型、发生时间、剩余可用时间和用户的保留选择。
- DecisionLog：每次重排的取舍、理由和用户是否接受，用于解释和后续改进。

### 暂定排程规则

1. 先排用户锁定的时间块和不可移动的日历事件。
2. 再按硬截止时间、用户优先级、预计时长和剩余时间计算可完成任务。
3. 留出缓冲，不把可用时间全部排满。
4. 时间不足时，将任务分为“必须保留”“建议延后”“暂不安排”，并要求用户确认取舍。
5. 重排只移动未锁定且未完成的任务，保留清晰的变更理由。

### 暂定任务类型与输入负担

- 固定事件：有确定开始/结束时间，默认不可移动。
- 硬截止任务：必须在指定时点前完成，但具体执行时间可排。
- 弹性任务：没有硬截止，可被延后或放入“有时间再做”。
- 重复任务：如运动、复习或家务，后续再确认是按日历重复还是完成后重新出现。

首版默认只要求标题、时长（可用快捷选项）和重要程度；截止时间、锁定状态和是否可延后按需补充。精力类型、地点、子任务、标签和复杂项目层级暂不强制录入，避免计划前的填写成本过高。

### 暂定可行性规则

- 可用时间中预留一部分缓冲，以应对任务估时偏差和临时事务。
- 当估算总时长超过当天可用时间时，系统跨天循环插入计划；若仍无法在截止前找到可行安排，应明确展示不可行和受影响任务。
- 一个长任务可建议拆成可开始的小块，但不应在未经用户确认时自动改变任务目标。
- 用户锁定的安排优先于系统推荐；硬截止优先于弹性任务。

### 暂定 MVP 里程碑

1. 本地任务录入、可用时间设置与今日计划展示。
2. 任务优先级/时长/截止时间、基础规则排程、手动拖动调整。
3. 恢复模式：打断后展示取舍并一键重排。
4. 本地通知、轻量复盘和历史记录。
5. 根据已确认的用户需要，加入课表/日历、习惯支持、账号/云同步和更深的云端 AI 能力。

## 讨论规则

- 不把工作假设当成产品决定。
- 用户明确认可、否定或选择的内容，才更新到“已确认决定”。
- 发生取舍时，记录选择结果和理由。
- 用户要求“总结思路文档”时，将本记录扩展为完整的产品思路文档。
- 若上下文接近上限，先生成一份不完整但覆盖所有已确认决定的接力文档。

## 变更记录

### 2026-09-11

- 建立 Repland 产品持续讨论记录。
- 确认产品名、Android 平台、核心理念和持续共创方式。
