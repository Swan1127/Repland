# Repland 粗略思路文档

## 1. 产品定位

Repland 是一款 Android 端的 AI 任务规划应用，面向容易拖延、感到任务失控或难以合理规划的人群。第一批核心用户是大一新生，重点帮助他们安排课程、学习、事务与休闲，降低制定计划的时间和脑力成本。

当前版本聚焦任务规划：把分散任务转化为可执行、可调整的计划。长期规划将在产品能力成熟后作为独立模块加入。

## 2. 核心价值

- 用 Agent 固化规划工作流、用 AI 提供分析与建议，减少用户反复与模型对话制定静态计划的负担。
- 让用户清楚知道当前该做什么，并在计划被打乱时得到新的可执行安排。
- 计划会随截止时间、可用时间、任务变化与执行反馈动态调整，而非一次生成后固定不变。
- 用户默认掌握任务和计划的最终控制权；拿不准时可以授权 AI 在既定范围内做规划取舍。

## 3. 第一版核心范围

- 快速收集任务，结合课表、休息时间和可用时间生成计划任务表。
- 依据硬约束与动态优先级安排任务，支持跨天拆分、冲突提示和重新规划。
- 通过完成反馈、每日总结、通知提醒、历史版本和回退帮助用户执行计划。
- 第一版为单设备工具，不含账号和跨设备同步；习惯支持和长期规划作为后续扩展。

## 4. 排序与计划原则

### 硬约束

- 不可避免事项、固定课程、临近截止任务及不可安排时间优先处理。
- 临近截止默认以三天内为范围，用户可调整并通过倒数日查看提醒。

### 软约束

- AI 任务规划适配度：45%。
- 用户高、中、低主观优先级：25%。
- 用户可修改的任务类别优先级：15%。
- AI 情境重要性：15%。

任务初始优先级为不可避免、高、中、低，默认中；不可避免属于硬约束。动态优先级会受截止、安排时段和多次延期影响，系统需要保留用户初始优先级并说明变化原因。

## 5. 用户控制与执行反馈

- 新任务产生冲突时，用户可手动调整，也可授权 AI 重排；重要变更保留历史版本并可回退。
- 任务完成、延期和取消状态只能由用户修改，AI 不替用户判定现实任务是否完成。
- 用户至少填写“完成了什么”，可补充耗时、进度、学习结果与成绩。
- 每天计划结束后，AI 根据真实任务状态和用户画像生成快速总结；通知用于提醒和监督，而不强迫用户执行。

## 6. 后续方向

- 基于长期执行反馈的按需习惯支持。
- 独立的长期规划模块。
- 在用户需求明确后，再考虑账号、同步、外部日历连接和更完整的隐私控制。
