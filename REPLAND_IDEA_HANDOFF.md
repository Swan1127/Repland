# Repland 阶段性不完整思路文档

> 这是上下文接力用的阶段性文档，不是最终需求文档。它汇总截至 2026-09-11 的全部讨论内容。只有“已确认决定”部分可以视为当前共识；候选方案、暂定草案和工作假设都需要用户后续确认。

## 一、已确认决定

- 产品名称：Repland。
- 目标平台：Android App。
- 核心理念：帮助用户梳理每天的任务和目标，并生成计划表。
- 项目背景（已确认的产品判断）：制定计划需要同时权衡任务、时间、目标与现实约束，往往消耗大量脑力和时间；而好的计划又是用户开始行动、取得成果的重要前提。现有待办和计划类 App 常停留在任务堆叠，难以随现实变化动态、个性化地调整。即使借助通用 Agent 制定计划，用户通常仍需多轮对话，得到的也往往是一份固定计划书。
- Repland 的问题定义：降低制定计划的时间、脑力与选择成本，以内置的 Agent 工作流持续生成并调整个性化计划，避免让用户反复通过对话维护一份静态计划书。
- 共创方式：通过持续提问逐步明确产品定位、用户流程、功能范围、交互方式和实现思路；用户认为可以时，再整理正式总结思路文档。
- 记录规则：明确认可、否定或选择的内容才升级为已确认决定；未确认内容必须保持为假设或候选方案。
- 长期面向容易拖延、感到任务失控或难以做出合理规划的人群。
- 第一批核心用户是大一新生：重点帮助他们安排学习与生活计划、形成执行习惯，并降低规划的时间与脑力成本。
- 用户主要在接到新任务后打开，也会在计划失控时回来调整；其余时间按需使用。
- 核心价值是把一堆任务安排进计划表，并以算法和 AI 结合任务事项、目标类型和优先级判断何事更重要，生成计划来帮助用户制定和执行计划。
- 今日计划生成器是主方向；重新规划与模糊目标拆解是辅助功能。
- 习惯培养面向有需要的部分用户（尤其大一新生）按需启用；它以用户的执行反馈为基础，由 AI 给出建立或调整习惯的建议，不应成为所有用户的必经流程。
- AI 参与任务理解、目标类型识别、优先级辅助判断和计划生成；可解释规则保障硬约束、计划可行性与用户控制权。
- 第一版核心范围：任务汇总与计划任务表。用户集中录入任务，Repland 对其排序并生成计划。
- 当前产品定位为任务规划工具；长期规划将在产品能力成熟后作为独立模块加入，而非当前任务规划功能的一部分。
- 当任务总量超过可用时间、且无法在截止前完成时，系统必须明确告知计划不可完全实现，并帮助用户在轮换、延期、拆分、降低目标或放弃之间取舍。
- 任务完成、延期和取消状态只能由用户自行改变；每天计划结束后，AI 可结合用户画像和当天任务状态生成快速总结，但不得改变任务状态。
- 总原则为“用户主导、可授权的 AI 决策”：默认由用户保留任务、目标和计划的最终控制权；当用户没有明确偏好时，可主动授权 AI 在既定范围内决定规划取舍。AI 始终不得违反硬约束、用户锁定事项和任务状态由用户维护的边界，且计划应可回退。
- 项目成功状态：用户对自己需要完成的事情有清晰认知，在复杂计划面前不再手忙脚乱或感到迷茫，并逐步形成更稳定的主动行动意愿与自驱力。
- Repland 是规划与执行助手：通过通知提醒和适度监督帮助用户回到计划，但不能替用户完成现实任务，也不能代替用户判断任务是否完成。
- 习惯培养不进入第一版的必须功能；保留为验证核心规划体验后的后续扩展方向。
- 第一版是单设备个人规划工具，不要求账号或跨设备同步。
- 软件以 Agent 固化任务理解、排序、硬约束检查、计划建议、变更预览和版本确认等工作流；AI 模型通过 API 参与具体分析和建议，从而降低接入不同模型时的体验差异。
- 第一版模型选择对用户透明，由 Repland 在后台统一管理；用户无需配置 API 或选择模型。

## 二、核心问题的候选理解

首要用户、使用场景和主方向现已确认。下面保留三种方向的原始讨论，并标记其当前定位：

### A. 今日计划生成器（核心）

用户输入今天要做的事、截止时间和可用时间，Repland 自动排序、估算时长并生成时间块。优势是价值直观、容易形成 MVP；风险是可能变成普通待办或日历工具。

### B. 目标拆解教练（辅助能力）

用户输入长期目标，Repland 将其拆成阶段目标、近期任务和今天的行动。优势是解决“知道想做什么，但不知道今天做什么”；风险是拆解质量和长期使用动力要求较高。

### C. 现实重排助手（辅助能力）

用户已有计划后，遇到加班、拖延或临时事务，通过很少的操作告诉 Repland，App 重新安排剩余任务，并解释哪些任务被保留、推迟或舍弃。优势是让计划适应现实，可能成为 Repland 的差异化；风险是需要可靠处理时间冲突、优先级和未完成任务。

当前已确认：以 A 作为产品核心，B 和 C 服务于更高质量的计划生成、执行和调整。

## 三、当前暂定设计草案（全部未确认）

### 暂定一句话定位

帮助大一新生把学习和生活的一堆任务，快速转成更合理、更容易执行的日计划。

### 暂定核心循环

接收新任务或目标 → AI 与规则识别类型和优先级 → 生成今日计划 → 执行与标记 → 必要时重新规划 → 按需复盘或进入习惯支持。

### 暂定首次使用入口

用户用自然语言输入类似“今天要完成报告、去健身、买日用品，报告最重要”的内容。Repland 提取任务，并只追问生成计划所必需的信息，例如截止时间、预计时长和不可用时间；随后生成可调整的时间块，而不是要求用户先建立复杂的项目体系。

### 暂定一日使用流程

1. 用户快速记录今天想做的事情，可以使用自然语言。
2. Repland 提取任务、截止时间、预计时长和优先级；只针对缺失且必要的信息提问。
3. 用户确认今天的可用时间，或直接采用默认可用时间。
4. Repland 展示“必须完成”和“如果有时间再做”两层计划，并可选生成时间块。
5. 用户通过完成、跳过、延后或“被打断”更新状态。
6. 发生变化时，用户点击“重新规划”，Repland 说明哪些任务被保留、移动或舍弃。
7. 晚间只做轻量复盘：完成了什么、未完成的原因、明天是否需要继续。

### 暂定输入方式

曾提出“自然语言输入框 + 自动识别后的结构化确认”：例如把“今天下午三点前完成报告，晚上去跑步，还要买猫粮”识别为任务、截止时间、时段和优先级，用户只修改错误部分。自然语言、表单、语音的主次尚未确认。

### 暂定产品原则

- 输入成本低于维护成本。
- 计划是建议而非承诺，用户始终可以修改、跳过或拒绝安排。
- 先保护最重要的事情；时间不足时主动暴露取舍。
- 计划失败不等于用户失败，重新规划应当是正常操作。
- AI 的关键判断要可解释，至少说明安排依据以及被延后的任务。

### 暂定差异化假设

- 重点不是让用户维护一份完美计划，而是让计划在被打乱后仍然有下一步行动。
- “重新规划”可能是产品名 Repland 最值得用户感知的能力。

### 暂定 MVP 边界

可能包含：任务快速录入；截止时间、预计时长、优先级和可用时间；今日计划自动生成；时间块拖动或手动调整；未完成任务的一键重新规划；计划执行状态和基础提醒。

暂不纳入的候选方向：复杂项目管理、社交协作、过度复杂的长期目标树、排行榜和装饰性功能。

### 核心用户叙事（场景细节待验证）

> 我刚进入大学，课程、作业、复习、生活琐事和想培养的习惯一下子变多。我不知道该把哪些事放进今天，也不想花很多时间自己排计划。Repland 帮我判断轻重缓急，生成可执行的安排；计划变化时，再帮我调整。

### 暂定成功标准

- 大一新生能在接到任务后快速把任务加入并获得可执行计划。
- 用户能理解 AI 为什么判断某些学习或生活任务更重要。
- 用户制定计划所需的时间和脑力降低。
- 用户能按计划推进重要事项；计划被打乱后能继续调整而非放弃。
- 对主动开启习惯支持的用户，产品帮助形成更稳定的学习与生活执行节奏。

### 暂定首页草案

- 顶部显示日期、剩余可用时间和轻量状态提示。
- 首要入口是“告诉我今天要做什么”的自然语言输入框，可附加语音入口。
- 计划主体按“现在/下一步”“今日必须完成”“有时间再做”组织；有时间条件时再显示具体时间块。
- 只突出一个最推荐的下一步，避免用户面对过长清单。
- 提供醒目的“重新规划”入口，目标是比手动编辑多个任务更省步骤。
- 底部导航是否包含今日、任务、目标、复盘尚未确认，可能需要进一步压缩。

### 已确认的计划表展示与渐进日程生成

用户默认先看到按动态优先级排列的任务清单。AI 根据后续完成情况、实际耗时和用户/学习画像，逐步优化为更具体的时间段日程。新用户或初步计划先以排序为主，把任务暂时插入课程等固定事件之外的空闲时段；这类初步安排因缺少个人反馈而参考性较弱，应被视为可调整建议。课程表与用户设置的休息时间共同界定不可安排范围，AI 只在其余可用时间内安排任务。随着反馈累积，日程的个性化程度和可信度提高，具体展示方式尚待确认。

已确认的任务清单行信息：任务名称、距截止日期的剩余天数；动态优先级作为轻量排序标注保留。没有截止日期时，明确应在当天完成的短期任务显示“本日内”；未给出时间信息的任务显示“待定”。

“待定”任务不参与 AI 自动排程，用户补充必要时间信息后才参与排序与计划生成。

## 八、当前方案的重大待解决问题

1. 输入与反馈负担：课表、休息模板、任务信息与每次完成后的总结，可能违背“降低规划成本”；需定义最小输入和渐进补充。
2. AI 可解释性与稳定性：45% AI 规划适配度和 15% AI 情境重要性需有结构化输入输出、理由与规则回退，避免不一致排序。
3. 动态优先级边界：提升阈值、更新频率、变化幅度和提示时机未定，需避免计划频繁跳动。
4. 估时与不可行性：需定义 AI/用户估时冲突、缓冲时间，以及一个月内无法满足截止时间时的告警和取舍。
5. 画像隐私：需确定数据保存、云端 AI 授权、用户查看/修改/删除画像和删除后的处理方式。
6. 版本规模：需确定历史版本留存、列表呈现，以及回退状态与近 7 天日志的关系。
7. MVP 收束：当前愿景包含排序、课表、休息、跨天排程、AI、画像、总结、版本和提醒，仍需划分首版必须项与验证后项。

## 九、第一版新建任务的最小输入

已确认方向：必填任务简介、初始优先级和时间长度；任务名称由 AI 从任务简介中提取/生成，简介也是 AI 分析任务难度和推进方式的主要上下文。用户能否编辑 AI 名称尚待确认。“时间长度”指用户预计完成任务所需的天数，与跨天任务机制一致。

## 十、文档粒度约定

当前阶段以粗略产品思路为主，不继续展开软件技术、数据字段、算法阈值或具体交互实现。已记录细化原则保留在本接力文档和产品记录中，待后续需要时再展开。简明思路见 `REPLAND_ROUGH_IDEA.md`。

休息时间以每周重复的固定模板为主，同时支持用户对当天单独临时修改。

已确认的容量不足与跨天插入规则：当天可用时间不足时，系统自动按天循环插入计划表；短期任务优先插入近期可用时段，长期任务可在多天间轮换分配工作量。自动安排必须遵守硬约束、截止日期、用户每日目标和可用时间；用户始终可手动调整日期、时间与分配方式。预计超过一个月完成的任务仍可正常加入并按天拆分；未来一个月只是计划表的可见滚动窗口，系统只展示该任务在近一个月内的每日事项。短期/长期同时依据用户填写的预计完成天数和 AI 对任务耗时、拆分需求的判断；实际排程优先使用用户填写的周期，AI 只展示建议周期及理由，用户可后续手动调整。

## 四、竞品验证与产品启示（2026-09-11）

- Todoist 已支持自然语言添加任务、时间块、日历同步和拖拽改期。
- Motion 已支持基于可用时间、时长、截止时间、优先级和日历变化的自动排程与重新优化。
- Structured 已主打任务与待办的单一可视化时间线。
- Sunsama 已支持引导式日计划、工作量预览、任务延后、时间块与自动重排。

参考来源：

- https://www.todoist.com/help/todoist/get-started/time-blocking-in-todoist-d6Pf1uTpc
- https://www.usemotion.com/help/time-management/auto-scheduling
- https://structured.app/
- https://www.sunsama.com/features/timeboxing

更新后的推断：自动生成计划已确定为 Repland 的核心价值，但成熟竞品已具备相近能力。需要进一步验证的差异化是面向大一新生的学习与生活场景、AI 对目标类型和优先级的辅助判断、以及按需习惯支持；恢复与重排作为重要保障能力。

### “恢复模式”候选体验

触发条件可以是用户主动说“我被打断了”或“今天来不及了”、排定任务超时、或新增日历事件造成冲突。Repland 先确认剩余可用时间、必须保留的任务和变化时长；接着清楚展示哪些任务保留、延后或放弃，而非静默改动；用户确认后，首页直接给出一个“现在开始”的下一步。

关键约束：不改变用户锁定任务或硬截止任务；不强行塞满剩余时间；每次重排解释原因；用户可以用极少操作恢复行动。该体验尚未确认。

## 五、MVP 实现思路（待确认）

- 原生 Android（Kotlin + Jetpack Compose），以任务录入、计划查看、课表/日程冲突处理和通知体验为优先。
- 本地优先、账号、同步和云端 AI 的边界尚未确认。
- 算法与 AI 协同：规则引擎处理锁定时间、固定事件、截止时间与可用时间等硬约束；AI 用于任务理解、目标类型识别、优先级辅助判断、计划建议和解释。用户可以修改计划。
- 核心排程采用可解释规则：锁定时间和固定事件优先，再基于截止时间、优先级、预计时长和剩余时间安排任务；保留缓冲，时间不足时明确展示“保留、延后、暂不安排”。
- 暂定任务类型为固定事件、硬截止任务、弹性任务和重复任务；首版只强制收集标题、快捷时长和重要程度，其余信息按需补充，避免录入负担。
- 暂定可行性约束为：预留缓冲、超过可用时间时明确提示不可行、优先尊重用户锁定安排与硬截止任务；长任务可建议拆分，但不静默改变目标。
- 最小数据对象包括任务、可用时间、计划时间块、打断事件和重排决策记录。
- 建议里程碑：任务录入与今日计划 → AI 辅助任务/目标理解和规则排程 → 手动调整与恢复模式 → 通知、轻量复盘、按需习惯支持 → 按用户需要再接入课表/日历、账号和云同步。

## 六、任务排序模型（正在共创）

已确认因素：用户对单个任务的主观优先级、任务类别，以及用户可修改各类别默认优先级。类别示例包括学习、工作、家务、游戏等。

主观优先级机制已确认：用户可选择不可避免、高、中、低四档初始优先级，默认“中”。其中“不可避免”是最高优先级选择，同时将任务提升为硬约束；高、中、低的初始判断占软约束评分的 25%。计划执行过程中，排序优先级会随时间流逝动态变化，计划表展示动态优先级；系统必须保留初始优先级，不能静默覆盖，并应展示变化原因。

已确认的动态优先级触发因素：接近截止时间、接近系统安排的执行时段、任务被多次延后。多次延后除提高动态优先级外，还应提醒用户重新调整该任务目标。

已确认的新任务重排方向：用户持续加入新任务时，AI 大模型能够同时比较新任务的初始优先级与旧任务的动态优先级，对全部未完成任务重新排序。模型需要考虑既有任务的截止风险、延后历史、计划时段、目标关联和初始意图。旧任务优先级的变化必须受到稳定性约束，避免计划因每个新任务频繁跳动；重要变化必须说明受影响旧任务和原因。

已确认的新任务冲突流程：新任务先加入计划表并保留冲突，不静默移动旧任务；系统在用户空闲时提示确认冲突；用户可选择自己手动排序，或授权 AI 对新旧任务重新排序。AI 只在用户选择 AI 排序后才生成重排建议，系统先展示受影响任务、变动内容和主要原因，用户确认后才应用。 “用户空闲”的判定方式尚待确认。

已确认的计划版本机制：原计划必须保留；用户或 AI 的每一次已确认计划改动都创建历史版本；用户可以回退到任一版本。建议采用非破坏性回退，即恢复旧版本时创建新的当前版本，同时保留后续历史，避免信息丢失；该实现细节待最终确认。

已确认的执行状态记录：完成、取消、延期等状态变化写入每日日志，日志只保留最近一周；同时每个留存的历史计划版本必须保存其创建时的任务状态，以便用户查看或回退时保留当时状态。回退旧版本时，任务状态也恢复到该版本当时的状态；回退应生成新版本以保留回退前的状态。历史版本的具体留存期限尚未确定。

候选稳定性约束：普通新任务不能挤掉硬约束、用户锁定或正在/即将执行的任务；旧任务无硬约束触发时不应一次跳变过大；已因截止风险或多次延后升级的任务不应被无解释地下调。新任务冲突在用户选择 AI 排序前不能触发自动调整。具体规则尚未确认。

所有任务由排序算法与 AI 大模型协同分析和排序：AI 结合任务内容、目标类型、类别和用户信息给出判断，算法将判断转化为可执行计划。

已确认的 AI 任务规划适配度：大模型分析任务难易程度、推进方式和目标发展节奏，判断任务更适合短期还是长期推进、需要集中整块时间还是拆为多段小时间完成。该因素占软约束评分的 45%。

已确认硬约束：临近截止时间的任务，以及用户标记为“不可避免”的任务。临近截止默认定义为距离截止时间不足 3 天；用户可以手动调整任务进入临近截止状态的日期/期限，并通过倒数日功能查看和接收提醒。用户可通过优先级选择把固定课程等不可跳过的事情从软约束提升为硬约束。第一版首次使用时，学生录入每周课表，课程自动成为重复、固定时间的不可避免任务。固定时间的不可避免任务直接占用对应时间块；没有固定时间的不可避免任务优先寻找可行时段。

设计原则：硬约束不以“更高分”参与普通竞争；系统必须先保证它们获得可行安排，其余可灵活任务再按加权分数排序。系统需解释主要排序理由，并允许用户覆盖建议。

待确认候选因素：截止时间/逾期风险在倒数期间的具体加权方式、预计时长、与长期目标或课程成绩的关联、任务依赖、用户锁定、多次推迟、可用时段与课程表、精力和任务难度匹配。

已确认的软约束权重：AI 任务规划适配度 45%；用户对单个任务设置的主观优先级 25%；用户可配置的任务类别优先级 15%；AI 补充情境重要性 15%。补充情境重要性由大模型综合判断任务与长期目标的关联、被拖延次数、课程/成绩影响及其他重要信号。客观历史与课程数据应由系统提供给大模型，模型负责综合解释而不凭空编造。AI 应输出难度、推荐推进周期、推荐执行形态、可选拆分建议及理由等可解释的结构化建议；算法再检查硬约束、可用时间和用户选择。

已确认的用户与学习画像方向：所有类别的任务完成后都留下简短总结；所有任务总结与实际执行记录构建通用用户画像，学习任务总结、学习结果和成绩额外构建学生学习画像。两类画像都是 AI 判断任务规划适配度和补充情境重要性的参考，必须以实际输入与实际记录为依据，避免凭空推断。画像的具体字段与隐私控制尚待确认。

已确认的完成总结内容：最低必填“完成了什么”；可补充实际耗时、详细完成结果、任务进度百分比和学习结果（如适用）。任务加入计划表时可补充详细介绍，作为 AI 任务规划适配度分析的重要上下文。AI 任务规划适配度维持已确认的 45%；“40%”属于口误。

已确认的跨天任务机制：用户创建任务时注明预计需要完成的天数，默认 1 天；多天长期任务按进度将剩余部分延续到下一天。若总结时进度不足 100%，任务保留已完成进度与剩余工作继续顺延，不被视为丢失或重新创建。预计天数、实际进度、实际耗时和剩余工作共同成为后续动态优先级与时间安排的参考。

跨天任务的每日分配支持两种方式：用户自行设定每日目标，或由 AI 依据总预计天数自动切分每日工作量。AI 会根据完成总结、实际进度、实际耗时和用户/学习画像持续校准后续每日工作量。用户每日目标与 AI 建议冲突时，AI 只能提出调整建议，必须等待用户确认，不能自动改变；确认后的调整应生成新的计划历史版本。

已确认的第一版默认任务类别画像（仅在“任务类别优先级”15%因素内部生效）：课内学习 30%，课外学习 25%，办公事务 25%，休闲娱乐 20%。用户可以修改类别优先级；“办公事务”的具体范围尚待确认。

## 七、尚未决定的问题

1. 对大一新生，第一版优先覆盖课程、作业、考试复习、社团/生活事务和习惯中的哪些内容？
2. 用户如何输入不可移动时间：手动填写课程表、导入课表/日历，还是两者都支持？
3. 计划表是精确时间块、有序任务列表，还是混合模式？
4. 用户如何表达目标类型和优先级，AI 在哪些情况下可以推断、哪些情况下必须询问？
5. AI 生成计划后，是先让用户确认、直接生成后修改，还是提供多个方案？
6. 任务超过可用时间时，系统如何呈现取舍和建议？
7. 用户未完成计划时，自动顺延、重新规划、分析原因，还是让用户手动处理？
8. 习惯培养以用户主动设置、AI 按需推荐，还是两者结合的形式出现？
9. Repland 的语气应偏温和陪伴、理性助手，还是严格教练？
10. 首版是否需要通知、课表/日历同步、账号、跨设备同步，以及云端 AI？

### 已提出但尚未确认的具体选项

- 计划严格程度：A 精确时间块；B 有序任务列表；C 混合模式。暂定建议为 C。
- 任务超出可用时间：A 按截止时间；B 按优先级；C 用户选出少量必须完成项；D 综合因素给出建议后由用户确认。暂定建议为 D。
- AI 生成后的主动程度：A 先展示草案并等待确认；B 直接生成后允许修改；C 提供多个方案；D 只提供建议。暂定建议为 A。
- 语气：A 温和陪伴；B 理性助手；C 严格教练。暂定建议为 A 为默认、必要时结合 B。
- 输入方式：A 自然语言文字；B 结构化表单；C 语音；D 自然语言为主、其他方式辅助。尚未确认，曾暂时偏向 D。
- AI 与隐私：A 默认云端；B 默认本地；C 基础功能本地、用户主动开启云端 AI；D 暂不使用 AI。暂定建议为 C。

## 八、下一步建议

优先让用户回答一个真实的一天：当时有哪些事情、哪里失控、希望 Repland 在那个瞬间具体做什么。然后据此确认首批用户、核心场景和 A/B/C 主方向，再继续细化首页、任务模型、排程规则和重新规划交互。

## 九、后续记录规则

- 用户明确认可的内容才移动到“已确认决定”。
- 用户否定的方案应记录为已排除，避免后续重复提议。
- 每次发生取舍，同时记录选择结果和理由。
- 用户要求正式总结时，在本阶段文档基础上整理完整思路文档。
