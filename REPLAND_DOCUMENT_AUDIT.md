# Repland 文档一致性审查报告

## 一、审查目的与范围

本报告以 `REPLAND_IDEA.md` 为当前主文档，核对以下三份文档中的内容：

- `REPLAND_FUNCTIONAL_IDEA.md`
- `REPLAND_IDEA_HANDOFF.md`
- `REPLAND_PRODUCT_NOTES.md`

审查目标是识别三类问题：

1. 已经确认、但尚未写入主文档的内容。
2. 主文档与其他文档之间存在的逻辑不一致或需要澄清的表述。
3. 历史记录中已经过期、仍可能造成误解的候选方案与失效引用。

本报告只记录检查结果，不代表已经修改任何被审查文档。

## 二、总体结论

`REPLAND_IDEA.md` 已覆盖 Repland 当前的大部分核心结论：项目背景、核心理念、用户人群、动态任务规划、重排、反馈、提醒、习惯支持、用户控制权和 Agent 原则均与最新决定基本一致。

不过，主文档目前更偏项目总览。若希望它也成为完整的确认版说明，还需要补入若干已经明确的运行规则。与此同时，`REPLAND_PRODUCT_NOTES.md` 和 `REPLAND_IDEA_HANDOFF.md` 是长期累积的讨论记录，保留了不少早期候选方案；它们不能在未经核对时直接作为当前结论使用。

## 三、主文档遗漏的已确认内容

### 1. 平台与第一版范围

以下内容在详细记录中已经明确，但 `REPLAND_IDEA.md` 尚未写明：

- Repland 的目标平台是 Android App。
- 第一版是单设备个人规划工具。
- 第一版不包含账号与跨设备同步。
- 第一版模型选择对用户透明，由 Repland 在后台统一管理；用户不需要配置 API 或自行选择模型。

这些信息主要来自 `REPLAND_FUNCTIONAL_IDEA.md`、`REPLAND_IDEA_HANDOFF.md` 和 `REPLAND_PRODUCT_NOTES.md` 的已确认决定部分。

### 2. 动态优先级的透明性与稳定性

主文档已记录动态优先级的触发因素和权重，但还遗漏以下已确认原则：

- 系统必须保留用户设置的初始优先级，不能静默覆盖。
- 动态优先级发生变化时，应向用户说明主要原因。
- 新任务加入后，AI 要同时比较新任务的初始优先级与旧任务的动态优先级。
- 旧任务的排序变化需要保持稳定，不能因每次新任务加入而频繁、大幅跳动。

这些规则的作用是避免 AI 排序变成无法理解、反复变化的黑盒。

### 3. 任务的“本日内”与“待定”状态

详细功能文档已明确两种时间信息不足时的处理规则，但主文档尚未包含：

- 没有截止日期、但明确需要在当天完成的短期任务，显示为“本日内”。
- 未提供必要时间信息的任务显示为“待定”。
- “待定”任务在用户补充必要信息前，不参与 AI 自动排程。

这组规则避免系统在信息不足时给出看似精确、但缺乏依据的自动安排。

### 4. 计划表与固定事项的细化规则

主文档已经说明计划任务表与硬约束，但尚未保留以下已确认细节：

- 计划任务表的基础信息为任务名称、距截止日期的剩余天数和轻量动态优先级标记。
- 有固定起止时间的“不可避免”事项直接占用相应时间块。
- 没有固定时间的“不可避免”事项，优先由系统寻找可行时段。

### 5. 已确认但尚未写入主文档的细节边界

以下内容也已在详细记录中出现，可视主文档未来定位决定是否纳入：

- 用户是否能够编辑 AI 生成的任务名称尚未确定。
- 用户画像和学习画像的查看、修改、删除、AI 授权及数据保存边界尚未确定。
- 课表或日历的导入能力尚未确定；当前已确定的是用户可录入课表。

上述内容不是“主文档漏掉的已确认功能”，而是仍需继续决定的开放问题。若主文档只承载已经确认的产品方向，可以不写入；若主文档也要承载风险与待决事项，则应补充为“待确定内容”。

## 四、需要澄清的逻辑关系

### 1. 当前任务规划与长期目标关联

当前结论是：Repland 现阶段是任务规划工具，长期规划以后作为独立模块加入。

但是，动态优先级的“AI 情境重要性”仍写有“任务与长期目标的关联”这一参考因素。此处存在需要澄清的边界：

- 第一版是否允许用户提供简单的个人目标背景，仅供 AI 判断任务重要性？
- 还是第一版完全不涉及长期目标，等长期规划模块出现后才使用该因素？

这并非已经确认的冲突，但需要明确数据来源，否则 AI 无法合理判断“长期目标关联”。

### 2. 长期规划模块与早期“目标拆解教练”

主文档已经明确：长期规划将在产品成熟后作为独立模块加入。

但 `REPLAND_PRODUCT_NOTES.md` 和 `REPLAND_IDEA_HANDOFF.md` 中仍保留“目标拆解教练”“接收新任务或目标”等早期候选描述。这些描述应被理解为历史讨论，而不是当前版本要实现的功能范围。

### 3. 通知与习惯支持的状态

当前结论已经明确：

- Repland 通过通知提醒和适度监督帮助用户回到计划。
- 习惯支持以后续按需扩展的形式存在，基于执行反馈提出建议。

但两份历史记录的“当前待确认问题”中仍保留“首版是否需要通知”“习惯培养采用何种形式”等提问。这些条目已经过期，需要避免被再次当作未决事项。

### 4. 新建任务最小输入的旧表述

当前已确认的新建任务最小输入是：任务简介、初始优先级、预计完成天数；AI 根据任务简介生成任务名称。

历史记录中还存在“标题、快捷时长、重要程度”为第一版最小输入的早期 MVP 候选。这与当前结论不一致，应仅作为被淘汰或待替换的历史方案保留。

### 5. 输入方式仍未确认

历史记录曾提出自然语言、表单、语音或三者组合等输入方式。

当前已确认的是任务需要提供任务简介、初始优先级和预计完成天数；但任务简介究竟以自然语言、表单还是语音为主要入口尚未最终确定。因此，历史文档中关于“首要入口是自然语言输入框”的描述不能视为当前结论。

## 五、历史文档中的失效引用

`REPLAND_ROUGH_IDEA.md` 已被删除，但以下文档仍引用它：

- `REPLAND_PRODUCT_NOTES.md` 的“文档粒度约定”部分。
- `REPLAND_IDEA_HANDOFF.md` 的“文档粒度约定”部分。

这些引用目前失效。后续如清理文档，应将它们替换为当前主文档 `REPLAND_IDEA.md`，或直接删除相关引用。

## 六、文档使用建议

建议按以下优先级使用现有文档：

1. **当前主文档：`REPLAND_IDEA.md`**
   - 用于审查项目背景、核心理念、用户人群、功能框架和项目意义。

2. **详细功能依据：`REPLAND_FUNCTIONAL_IDEA.md`**
   - 用于查阅动态优先级、任务排程、反馈、版本、通知与 Agent 工作流的已确认细则。

3. **完整讨论与接力记录：`REPLAND_PRODUCT_NOTES.md`、`REPLAND_IDEA_HANDOFF.md`**
   - 用于追溯讨论来源、候选方案和未决问题；阅读时必须区分“已确认”“候选”“待确认”和“早期草案”。

## 七、审查后建议处理项

以下处理项仅供后续审查决定，尚未执行：

1. 决定是否把第三节中的“已确认遗漏”补入 `REPLAND_IDEA.md`。
2. 决定第一版是否允许“长期目标背景”参与任务排序。
3. 清理历史记录中已过期的待确认问题和旧 MVP 候选，或为其明确标记“已废弃”。
4. 修复两份历史文档对已删除粗略思路文档的引用。
