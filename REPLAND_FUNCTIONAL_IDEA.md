# Repland 功能思路文档（当前版本）

## 1. 产品定位

Repland 是一款 Android 端的 AI 计划规划应用，帮助用户把分散、难以判断轻重缓急的任务整理为可执行计划。

长期面向容易拖延、感到任务失控或难以合理规划的人群。第一批核心用户是大一新生，重点帮助他们安排课程、作业、学习、事务与休闲，降低制定计划的时间和脑力成本。

用户主要在接到新任务后使用 Repland；当计划发生冲突或失控时，也可以回来调整。

## 2. 第一版目标与边界

第一版的目标是验证“任务汇总 → AI 与算法排序 → 生成计划任务表”是否能真正帮助大一新生更轻松地规划和执行。

第一版包含：

- 任务汇总与计划任务表。
- 课表、休息时间与可用时间管理。
- 任务排序、跨天安排与新任务冲突处理。
- 完成反馈、近期日志与计划历史版本。
- 计划提醒与执行监督。
- Agent 驱动的 AI 分析与计划建议。

第一版暂不包含：

- 习惯培养。该思路在核心规划体验获得验证后，再面向有需要的用户加入；它基于执行反馈生成建议，帮助用户逐步形成稳定习惯。
- 长期规划。当前版本聚焦任务规划；产品成熟后，长期规划将作为独立模块加入。
- 账号与跨设备同步。第一版是单设备个人规划工具。

## 3. 初次使用与时间范围

用户首次使用时录入每周课表。课程自动成为重复、固定时间的“不可避免”任务。

用户还需要设置休息时间：以每周固定模板为主，同时支持当天临时修改。课表与休息时间共同构成不可安排范围，Repland 只在其他可用时间中安排任务。

## 4. 新建任务

第一版新建任务的最小输入为：

- 任务简介。
- 初始优先级。
- 预计完成天数，默认 1 天。

AI 根据任务简介生成任务名称，并将任务简介作为理解任务难度、推进周期、拆分需求与合适执行方式的重要依据。

用户可选择“不可避免”优先级，使任务从普通排序因素提升为硬约束。固定课程是典型的不可避免任务。

## 5. 任务类别与初始优先级

第一版预置四类任务。用户可以修改类别优先级：

| 任务类别 | 类别优先级因素内的默认比例 |
|---|---:|
| 课内学习 | 30% |
| 课外学习 | 25% |
| 办公事务 | 25% |
| 休闲娱乐 | 20% |

用户可为每个任务设置不可避免、高、中、低四档初始优先级，默认值为“中”。其中“不可避免”既是最高优先级选择，也会使任务成为硬约束；高、中、低则表达用户的普通主观优先级。

## 6. 排序与计划生成

### 硬约束

- 临近截止时间的任务：默认定义为距截止不足 3 天，用户可以按任务修改该期限。
- 不可避免任务：如固定课程或用户主动标记为不能跳过的事项。

临近截止任务通过倒数日功能提醒用户。硬约束不与普通权重竞争，系统应优先为其保留可行安排。

### 软约束

在不考虑硬约束后，普通任务的排序参考以下权重：

| 因素 | 权重 | 作用 |
|---|---:|---|
| AI 任务规划适配度 | 45% | 判断任务难度、短期或长期推进、整块专注或分段完成等。 |
| 用户主观优先级（高/中/低） | 25% | 保留用户对单个任务的重要性判断；“不可避免”作为硬约束，不参与此项加权。 |
| 任务类别优先级 | 15% | 使用用户可修改的类别默认倾向。 |
| AI 补充情境重要性 | 15% | 参考长期目标关联、拖延情况、课程/成绩影响与用户画像。 |

AI 负责理解、分析和解释；确定性规则负责检查课表、休息、截止日期、不可避免任务与可用时间，确保计划可行。

## 7. 动态优先级

计划表展示动态优先级，而非只展示用户最初设置的高、中、低。

以下情况会提高动态优先级：

- 截止日期临近。
- 系统安排的执行时段临近。
- 任务被多次延后。

被多次延后的任务除了提高优先级，还应提醒用户重新调整任务目标。系统需保留用户初始优先级，并说明动态优先级变化的原因，不能静默覆盖用户意图。

## 8. 计划任务表与跨天安排

用户打开当天计划时，默认先看到按动态优先级排序的任务清单。单行重点显示：

- 任务名称。
- 距截止日期的剩余天数。
- 轻量的动态优先级标注。

没有截止日期但明确应在当天完成的短期任务显示“本日内”；未提供时间信息的任务显示“待定”。待定任务不参与 AI 自动排程，直到用户补充必要时间信息。

时间日程会在完成反馈积累后由 AI 逐步优化。初期时间安排只是将任务暂时插入课程和休息之外的空闲时段，属于可调整建议；随着实际完成情况、耗时和画像积累，日程会更贴合个人节奏。

当当天容量不足时：

- 短期任务优先进入近期空闲时段。
- 长期任务可以在多天之间轮换分配工作量。
- 超过一个月的任务仍可正常按天划分，但计划表仅展示最近一个月内的每日事项。
- 用户始终可以手动调整任务所在日期、时间与分配方式。

如果任务总量超过可用时间，且无法在截止前得到可行安排，系统必须明确提示计划无法全部完成，并帮助用户在轮换、延期、拆分、降低目标或放弃之间做出取舍，而不是继续给出看似可行的排序。

短期与长期任务同时参考用户填写的预计完成天数和 AI 对任务耗时、拆分需求的判断。实际排程优先使用用户填写的周期；AI 只给出建议周期和理由，用户可自行调整。

跨天任务支持两种每日分配方式：用户设置每日目标，或让 AI 自动切分。AI 会根据完成反馈和用户画像提出后续调整建议，但不能自动改写用户设定的每日目标。

## 9. 新任务、冲突与重排

新任务加入时，系统先将其放入计划表并保留与旧计划的冲突，不静默移动原有任务。

用户确认冲突后可以：

- 自己手动排序和调整。
- 选择让 AI 对新旧任务重新排序。

AI 重排必须先展示受影响任务、变动内容与主要原因；用户确认后才应用。当用户对取舍没有偏好时，也可主动授权 AI 在既定范围内直接决定重排结果。无论是否授权，普通新任务都不能无解释地挤掉硬约束、用户锁定或即将执行的任务，且任务状态仍只能由用户改变。

## 10. 完成反馈、用户画像与学习画像

每个任务完成后，用户至少记录“完成了什么”。用户也可以补充：

- 实际耗时。
- 具体完成结果。
- 任务进度百分比。
- 学习结果和成绩（适用于学习任务）。

所有任务的完成反馈与实际执行记录形成通用用户画像；学习任务反馈、课表、学习结果与成绩形成学习画像。两类画像均可作为 AI 判断任务规划适配度与情境重要性的参考，但必须基于实际记录，不应凭空推断用户能力或状态。

完成进度未达到 100% 的任务会保留已完成进度和剩余工作，延续到下一天，而不会丢失。

任务是否完成、延期或取消只能由用户自行改变状态；AI、系统和算法只能依据用户状态提供分析与建议，不能替用户判定任务状态。每天计划结束后，AI 根据用户画像和当天任务的实际状态快速生成当日总结，但总结不改变任务状态。

在后续习惯支持中，连续的执行反馈还可用于识别用户的真实节奏、常见中断与可改善之处，并由 AI 提供建立或调整习惯的建议；是否采用始终由用户决定。

## 11. 日志与计划版本

- 完成、取消、延期等执行状态变化写入每日日志，日志保留最近 7 天。
- 原计划和每次用户或 AI 已确认的计划改动都形成历史版本。
- 用户可以回退到任一历史版本。
- 回退时恢复该版本当时的任务排序、时间安排和完成状态；回退本身生成新版本，以保留回退前状态。

## 12. 计划提醒与执行监督

Repland 通过通知提醒用户关注即将执行、临近截止或需要重新处理的计划事项，帮助用户回到计划并持续推进。

它始终是规划与执行助手：不能替用户完成现实任务，也不能自动确认、延期或取消任务状态；这些状态只能由用户自行改变。

## 13. Agent 与 AI 模型的关系

Repland 以 Agent 作为稳定工作流基座。Agent 固化任务理解、排序、硬约束检查、计划建议、变动预览与版本确认等流程。

AI 大模型通过 API 参与具体的任务分析、优先级建议、周期建议、拆分建议和解释。这样未来即使接入不同模型，产品工作流和输出标准也尽量保持一致，降低模型差异对用户体验的影响。

## 14. 后续需要继续确定的方向

- 首页应优先呈现“今天可立刻执行的清单”还是“全部任务的全局总览”，以及两者如何切换。
- “办公事务”的具体范围。
- 用户是否可以编辑 AI 生成的任务名称。
- 用户画像和学习画像的隐私、查看、修改、删除与 AI 授权方式。
- AI 建议的具体呈现方式与可信度提示。
- 日历连接、账号与同步是否在后续版本加入。
