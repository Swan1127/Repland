# Repland 产品框架与用户工作流程（当前讨论基线）

> 本文只定义产品的整体结构和用户流程，不继续展开任务字段、算法权重、页面控件等细节。已确认的细化原则仍保存在其他思路文档中，后续进入对应模块时再调用。

## 1. 产品的一句话定义

Repland 是一款面向容易拖延、失控或不会规划的人群的安卓计划工具：它把用户脑中的任务收拢起来，结合个人时间与 AI 判断生成可执行计划，并在现实变化后帮助用户重新安排。

第一阶段主要服务大学大一新生，帮助他们把课程、学习、事务和休闲放入可执行的日常计划中。

## 2. 核心闭环

```text
收集任务 → 理解与规划 → 查看并确认计划 → 执行与反馈 → 根据变化重新规划
      ↑                                                      ↓
      └────────────── 个人规律与计划历史逐步沉淀 ──────────────┘
```

Repland 的价值不在于单纯记录待办，而在于把“杂乱任务”变成“当前应做什么”，并让计划可以随着实际情况持续调整。

## 3. 第一版的五个核心功能模块

### 3.1 任务收集

用户将新出现的学习、课程相关、事务或休闲事项快速加入 Repland。这里的目标是先接住任务，降低“我得先想清楚才能记录”的压力。

### 3.2 时间与约束设置

用户告诉 Repland 自己不可安排的时间，例如课程和休息时间。系统据此识别哪些时间能够用于执行任务。

### 3.3 AI 规划与计划生成

系统综合任务的重要程度、时间要求、个人偏好及可用时间，对任务进行排序和分配，形成计划任务表。AI 提供分析和建议，规则保证不可违反的约束被遵守。

#### 规划的两层优先级

每个任务先有一层由用户表达的**初始优先级**：不可避免、高、中、低，默认中。其中“不可避免”是用户对不能跳过事项的最高声明；它同时把任务纳入硬约束。其余三档代表用户当下的主观判断，不会被系统悄悄改写。

在生成和更新计划时，系统还会计算一层面向行动的**动态优先级**。它决定任务当前在计划表中的位置，并会随现实变化更新。用户仍能看到其变化原因和原本设定的优先级。

#### 先处理不能违反的条件

以下属于硬约束，优先于一般排序：

- 用户标记为“不可避免”的事项，例如固定课程。
- 接近截止日期的事项；默认以三天内为临近区间，用户可调整，并通过倒数日提醒。
- 用户的课程和休息时间所划出的不可用时段。

硬约束的作用不是让所有任务都显得紧急，而是保证计划不会把必须完成、马上到期或无法安排的现实条件忽略掉。

#### 在可安排任务中判断什么更重要

除硬约束外，系统以 AI 与规则共同判断任务的排序，当前已确认的判断结构为：

| 判断来源 | 当前占比 | 所解决的问题 |
| --- | ---: | --- |
| AI 任务规划适配度 | 45% | 任务难度、预估耗时、适合短期或长期、需要连续专注还是可拆成多段完成。 |
| 用户主观优先级（高/中/低） | 25% | 保留用户对“这件事对我重要”的直接判断；“不可避免”不参与此项加权，而是直接作为硬约束。 |
| 任务类别优先级 | 15% | 反映用户对不同生活领域的长期偏好。 |
| AI 情境重要性 | 15% | 结合长期目标关联、延期情况、课程与成绩影响及画像判断。 |

第一版面向大一新生时，预置类别优先比例为：课内学习 30%、课外学习 25%、办公事务 25%、休闲娱乐 20%；用户可以按自己的生活安排修改。

#### 动态优先级如何变化

动态优先级会在以下情形上升：截止日期越来越近、已安排的执行时段越来越近、任务被多次延后。多次延后不仅意味着排序上调，也说明原目标或安排可能不现实，系统应提示用户重新评估任务。

新任务加入后，AI 需要把它的初始判断与旧任务已经变化的动态优先级一起比较。普通新任务不能无理由挤掉硬约束、用户锁定或即将执行的旧任务；这使计划既能应对新情况，也不会持续推翻已有安排。

#### 从排序变为可执行计划

排序后，系统将任务放入课程和休息以外的可用时间，并结合用户预估完成天数与 AI 的拆分判断安排任务。短期任务优先安排到近期空闲时间；长期任务可在多天之间分配或轮换。用户设定的完成周期和每日目标优先，AI 只提出调整建议，不擅自更改。

第一份时间日程只是初步建议：它可以先把任务放进可用空档，随着真实完成反馈不断变得贴合个人节奏。

### 3.4 今日执行

用户打开 App 后，优先获得当前需要行动的任务清单；需要时可查看任务的整体情况和初步日程。用户在执行中完成、延后或调整任务。

今日清单是动态优先级真正面向用户的输出：用户应知道当前该做什么、何时需要做，以及任务为什么被排在这里。任务名称、剩余截止天数和轻量优先级提示构成基本行动信息；没有明确时间信息的任务保持“待定”，在补充信息前不参与自动排程。

执行并不是与规划分离的一次性步骤。用户完成任务、只能完成一部分或决定延后时，系统保留实际进度和剩余工作，并把变化带回后续计划。未完成工作可延续到下一天，而不会被视为消失或简单失败。

### 3.5 反馈、调整与回溯

完成或未完成都会成为下一次规划的依据。发生冲突、临时新增事项或计划失控时，用户可以选择自己调整，或让 AI 提供重排方案。每次确认的计划都能回溯。

#### 用反馈校正未来判断

任务完成时，用户至少说明“完成了什么”，也可补充耗时、完成结果、进度、学习结果或成绩。所有任务反馈逐步形成对个人执行节奏的认识；学习相关记录还会形成学习画像。它们是 AI 判断任务难度、安排方式和情境重要性的依据，但必须来自真实记录，而不是凭空给用户贴标签。

#### 计划失控时的重排方式

当新任务插入、任务延期或可用时间发生变化，系统先保留冲突并说明受影响内容，而不是静默改动旧计划。用户随后可以自己调整，也可以要求 AI 生成重排建议。AI 必须展示主要变化和原因，用户确认后才生效。

#### 用历史保障用户控制权

每次用户或 AI 确认计划改动都保留为版本，用户可以回到任何一个历史计划。回退会恢复当时的排序、时间安排和完成状态，并同时生成新的版本以保留回退前状态。

执行中的完成、取消和延期等变化会形成每日日志，近七天可供回顾。这让动态优先级、计划变化和真实执行结果之间保持可追溯，而不是成为不可理解的黑盒。

## 4. 用户主工作流程

### 流程 A：首次开始使用

```text
进入 Repland
  → 设置课程与日常休息时间
  → 写入近期想做或必须完成的任务
  → 系统生成第一份初步计划
  → 用户查看、调整并确认
```

第一份计划的定位是可修改的起点，而不是一次性给出绝对正确的时间表。

### 流程 B：日常使用

```text
打开 App
  → 看到今天优先应处理的任务
  → 开始执行
  → 完成后简单反馈完成情况
  → 系统逐步理解用户的节奏，并优化后续安排
```

这条流程应尽量短，让用户把注意力放在行动上，而不是维护计划本身。

### 流程 C：临时新增或失控时

```text
收到新任务 / 发现计划无法完成
  → 加入或调整任务
  → 系统指出冲突和受影响的计划
  → 用户选择自行调整，或查看 AI 重排建议
  → 确认新计划，并保留旧版本
```

这条流程是 Repland 与普通待办工具的关键差别：计划不是被打乱后作废，而是可以被有依据地重新安排。

### 流程 D：按需习惯支持（后续扩展）

```text
持续执行与完成反馈
  → 系统识别用户的实际节奏、常见中断和可改善之处
  → AI 给出建立或调整习惯的建议
  → 用户自主选择是否采用
  → 在后续执行反馈中持续调整建议
```

习惯支持不应要求所有用户额外维护一套流程，也不应只靠打卡。它以既有任务的执行反馈为基础，在用户需要时提供更适合其真实节奏的行动建议，帮助形成稳定的执行习惯。

## 5. 产品中的三类输出

用户最终从 Repland 获得三种东西：

1. **行动答案**：今天或当下优先该做什么。
2. **计划全景**：所有任务如何被安排、哪些存在冲突或风险。
3. **调整建议**：当任务新增、延期或完成情况变化时，下一步如何重新安排。

## 6. 第一版边界

第一版验证“任务收集—AI 规划—执行反馈—重新规划”这一闭环是否有价值。

习惯培养、长期成长陪伴、跨设备同步和更完整的外部日历连接，都属于后续扩展方向；其中习惯支持已明确为流程 D：它从执行反馈中产生建议，由用户按需开启。它们不应遮蔽第一版的核心：让用户更省力地制定并执行计划。

## 7. 接下来应讨论的层级

后续讨论优先按以下顺序推进：

1. 用户从首次打开到获得第一份计划的整体体验。
2. 用户每天打开 Repland 时，如何从“看到计划”自然进入“开始执行”。
3. 用户的计划被打乱时，重排流程如何让人感到可控。
4. 每个模块需要哪些最小能力，随后才讨论具体规则、字段和交互细节。
