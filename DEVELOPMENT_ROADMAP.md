# Repland 开发任务链

> 本文是后续开发的执行顺序与验收清单。除非产品规则发生明确变更，新增功能应按本任务链推进。

## 核心定位

Repland 是以 **受限规划 Agent** 为基座的个人任务规划应用，而不是自由聊天机器人。

Agent 可以理解任务、检查约束、提出排序、拆分与重规划建议；但只有用户能确认任务状态和计划变更。所有 AI 结果都必须是可查看、可编辑、可丢弃的草案。

```text
任务与时间约束
  → 分析与排程（本地规则 / AI Agent）
  → 计划草案
  → 用户编辑并确认
  → 当前计划、提醒与执行
  → 用户反馈
  → 调整草案
```

## 不可违反的规则

- 未确认草案不能替换当前计划，也不能触发提醒。
- AI、规则与系统不能自行完成、延期、取消或替换任务。
- 课程、休息、用户锁定时间段和不可避免任务是硬约束。
- 历史计划和执行日志只能追加或由新记录更正，不能静默覆盖。
- AI 不可用或用户关闭 AI 时，本地确定性规划闭环仍必须可用。
- 联网 AI 前必须取得用户明确同意，只发送当前任务及必要的结构化上下文。

## 任务链

### 0. 开发与测试基线

**目标：** 保证每项功能都能在模拟器和真实设备上回归验证。

- 使用现有 API 36 模拟器完成编译、安装与调试。
- 新建 API 24 或 API 26 模拟器，覆盖项目最低支持版本。
- 准备至少一台真机，验证通知、后台限制、文件选择和 PDF 导入。
- 保持 `assembleDebug`、单元测试和仪器测试可执行。

**验收：** API 36 与低版本环境均可安装启动；核心构建和测试命令通过。

### 1. 任务生命周期与版本化执行日志

**目标：** 让每一次用户确认的执行结果可追溯，并为重规划提供可靠输入。

- 完整支持未开始、进行中、已完成、已延期、已取消、已替换状态。
- 支持部分完成、实际耗时、进度、完成内容、结果与延期原因。
- 每次用户状态确认创建独立执行日志；更正以新日志记录而非覆盖旧记录。
- 替换任务时保留原任务、原计划和执行历史。

**验收：** 对任意任务的状态改变、部分完成和更正，都能按时间顺序还原原因与结果。

### 2. 时间约束与课表可靠性

**目标：** 让计划建立在真实可用时间上。

- 完善每周课程、休息和其他重复时间块。
- 完善单日可用/不可用例外、单双周规则与用户锁定时间段。
- PDF 课表导入后必须提供预览、修改和确认，确认前不得写入固定约束。
- 在没有足够时间信息时，只生成排序任务清单，不编造具体时间段。

**验收：** 排程不会与已确认硬约束重叠；课表错误可在确认前修正。

### 3. 计划草案、手动编辑与历史回退

**目标：** 把排程结果变成用户可控的计划版本。

- 草案展示任务排序、时间段、未排入任务和容量不足原因。
- 支持拖动排序、移动时间段、拆分/合并工作段、锁定时间段。
- 支持确认、丢弃草案；确认后创建新的当前计划版本。
- 支持查看历史计划；恢复历史必须创建新版本，不能删除已有历史。

**验收：** 用户可完整审阅并编辑任何变更；未确认草案不影响当前执行计划。

### 4. 本地动态优先级与确定性排程

**目标：** 在没有 AI 时仍生成可解释、可测试的高质量计划。

- 按 30 分钟粒度和滚动 30 天窗口排程。
- 将用户初始优先级、类别偏好、截止/逾期、延期次数和稳定平局规则纳入排序。
- 先检查硬约束和总容量，再执行软排序。
- 对容量不足的任务明确说明原因，并保留待安排任务，不静默删除或移动。
- 为排序和未排入结果提供简短解释。

**验收：** 相同输入始终获得相同结果；不与硬约束冲突；容量不足能被明确解释。

### 5. 执行反馈与调整草案

**目标：** 完成“计划—执行—反馈—重规划”的本地闭环。

- 已结束工作段进入“待确认”，不自动判定成功或失败。
- 用户提交完成、延期或部分完成反馈后，计算剩余工作。
- 新任务、反馈和未来约束变化均生成调整草案，而非自动修改当前计划。
- 累计延期达到阈值时，提供拆分、降低目标、改期等选择；最终决定仍由用户确认。

**验收：** 计划被打断后，原历史仍在，剩余工作不会丢失，新的安排必须经用户确认。

### 6. 本地提醒与每日回顾

**目标：** 在不强迫用户的前提下支持行动和复盘。

- 仅为已确认计划创建工作段开始、临近截止和每日回顾提醒。
- 在计划确认、修改、回退或取消后正确创建、更新和撤销对应提醒。
- 支持每日汇总：完成情况、实际耗时、进度和下一步建议。
- 跳过回顾不得推断任务状态。

**验收：** 提醒不会来自草案或已失效计划；用户可关闭提醒且不影响核心功能。

### 7. 画像、数据管理与导出

**目标：** 让长期规划证据可控、可检查、可带走。

- 基于用户确认的执行反馈生成通用画像和学习画像证据。
- 用户可以查看、修正和删除不准确的画像结论。
- 支持本地 JSON 导出。
- 支持明确确认后的本地数据清除；已导出的文件不受影响。

**验收：** 用户能解释任何画像结论的来源，并能导出或清除本地数据。

### 8. 受限 AI Agent 层

**目标：** 在不破坏本地闭环与用户控制权的前提下加入高级建议。

- 定义版本化、类型化的 AI 契约：任务理解、难度、时长、拆分、排序解释、重规划和每日总结。
- 在应用内实现明确的首次授权、AI 开关、请求数据预览和失败提示。
- 只发送当前任务内容和必要的相关结构化上下文。
- 本地验证 AI 输出；输出只能形成建议数据和计划草案，不能直接写任务状态。
- 保留本地确定性降级模式，并通过测试保证两种模式的边界一致。

**验收：** 断网、AI 失败或关闭 AI 时应用仍可完成本地规划；AI 不会越权修改用户数据。

### 9. 质量、隐私与发布准备

**目标：** 让应用达到可内测、可维护、可回归的质量门槛。

- 为排程、约束、迁移、日志、回退和 AI 输出校验补足单元测试。
- 为任务录入、草案确认、执行反馈、提醒和 PDF 导入补足仪器/UI 测试。
- 配置 release 签名、版本号规则、混淆策略和内测构建。
- 建立构建与测试 CI；加入崩溃报告前先明确数据收集与隐私说明。
- 在真实设备上回归通知、后台限制、时区、文件访问和升级迁移。

**验收：** 可生成受签名保护的内测包；核心路径有自动化测试；升级不会损坏用户本地数据。

### 10. 理想版扩展（明确进入下一产品阶段后再做）

**目标：** 在 V1 验证通过后扩展服务能力，而不是提前给 MVP 引入复杂性。

- 受控 AI 后端或边缘函数：密钥只保存在服务端，不写入 APK。
- 监控、限流、审计、模型评估和成本控制。
- 可选账户与跨设备同步：身份认证、加密、冲突处理、备份和数据删除流程。
- 基于真实反馈的按需习惯支持与长期目标模块。

**验收：** 扩展能力不削弱离线可用性、用户控制权或隐私边界。

## 实施顺序

```text
0 开发基线
  → 1 生命周期与日志
  → 2 时间约束
  → 3 草案与手动编辑
  → 4 本地排程
  → 5 执行反馈与重规划
  → 6 提醒与回顾
  → 7 画像、导出与数据管理
  → 8 受限 AI Agent
  → 9 质量与发布
  → 10 理想版扩展
```

## 当前起点

仓库已经具备 Kotlin、Compose、Room、本地任务与时间约束、PDF 课表导入、基础计划草案和本地排程器。因此后续实现从 **任务 1：任务生命周期与版本化执行日志** 开始；不要重做现有基础架构。
